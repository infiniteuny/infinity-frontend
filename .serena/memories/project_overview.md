Project: Infinity frontend web app (Next.js App Router). Repo root: E:\INFINITE\infinity\frontend. Clean architecture with DI (inversify). Structure: app/ (routes), src/domain (entities, repo contracts), src/application (use cases), src/infrastructure (repo/datasource impl), src/presentation (controllers/components/stores/hooks), config/ (symbols, themes, fonts). Composition roots in src/server-injection.ts and src/client-injection.ts. Path aliases: @app/* -> src/*, @config/* -> config/*.