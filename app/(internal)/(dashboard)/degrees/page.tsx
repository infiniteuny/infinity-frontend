import { SectionHeader } from '@app/presentation/components/internal/shared';

export const dynamic = 'force-dynamic';

export default async function DegreesPage() {
  return (
    <>
      <SectionHeader title="Degrees"></SectionHeader>
    </>
  );
}
